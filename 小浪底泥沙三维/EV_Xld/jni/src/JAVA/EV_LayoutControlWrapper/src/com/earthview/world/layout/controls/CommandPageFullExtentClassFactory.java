package com.earthview.world.layout.controls;

import global.*;
import com.earthview.world.base.*;
import com.earthview.world.util.*;
import com.earthview.world.core.*;

public class CommandPageFullExtentClassFactory implements IClassFactory {
	public BaseObject create()
	{
		CommandPageFullExtent emptyInstance = new CommandPageFullExtent(CreatedWhenConstruct.CWC_NotToCreate);
		return emptyInstance;
	}
}
